# ─── Server ───────────────────────────────────────────────
NODE_ENV=development
PORT=5000

# ─── MongoDB Atlas ────────────────────────────────────────
# Get this from your Atlas cluster → Connect → Drivers
# Format: mongodb+srv://<user>:<password>@cluster.mongodb.net/wattwise?retryWrites=true&w=majority
MONGO_URI=localhost:27017

# ─── Firebase Admin SDK ───────────────────────────────────
# Option A — Service account JSON file (recommended for local dev)
#   1. Go to Firebase Console → Project Settings → Service Accounts
#   2. Click "Generate new private key" → download the JSON
#   3. Place it somewhere OUTSIDE your repo (never commit this file!)
#   4. Set the absolute path below
FIREBASE_SERVICE_ACCOUNT_PATH=config/serviceAccountKey.json

# Option B — Individual env vars (recommended for production / CI)
#   Uncomment these and leave FIREBASE_SERVICE_ACCOUNT_PATH blank
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"

# ─── CORS ─────────────────────────────────────────────────
# Comma-separated allowed origins. Leave blank to allow all (dev only).
ALLOWED_ORIGINS=
