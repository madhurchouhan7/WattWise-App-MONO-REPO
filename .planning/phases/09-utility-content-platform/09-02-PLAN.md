---
phase: 09-utility-content-platform
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - backend/src/models/UtilityContent.model.js
  - backend/src/controllers/content.controller.js
  - backend/src/routes/content.routes.js
  - backend/src/routes/index.js
  - backend/src/middleware/validation.middleware.js
  - backend/tests/content.contract.test.js
  - backend/tests/content.cache.test.js
autonomous: true
requirements:
  - CNT-01
  - CNT-02
  - CNT-03
  - CNT-04
  - CNT-05
must_haves:
  truths:
    - "Backend serves FAQ, bill guide, and legal content from published data using normalized envelopes."
    - "FAQ endpoint returns relevant search/filter results for q/topic queries with deterministic behavior."
    - "Refresh requests can return 304 when content is unchanged and 200 with new metadata when updated."
  artifacts:
    - path: "backend/src/models/UtilityContent.model.js"
      provides: "Versioned content schema for faq/bill_guide/legal payloads"
    - path: "backend/src/controllers/content.controller.js"
      provides: "Content fetch handlers with metadata and conditional GET behavior"
    - path: "backend/src/routes/content.routes.js"
      provides: "Mounted content endpoints and validation wiring"
  key_links:
    - from: "backend/src/routes/index.js"
      to: "backend/src/routes/content.routes.js"
      via: "route mount under /api/v1/content"
      pattern: "router.use('/content'"
    - from: "backend/src/middleware/validation.middleware.js"
      to: "backend/src/routes/content.routes.js"
      via: "query/param validation for faqs/legal"
      pattern: "getFaqContent|getLegalContent"
    - from: "backend/src/controllers/content.controller.js"
      to: "backend/tests/content.cache.test.js"
      via: "etag and conditional refresh assertions"
      pattern: "ETag|If-None-Match|304"
---

<objective>
Implement the backend utility content platform with versioned content, deterministic filters, and conditional refresh semantics.

Purpose: Deliver backend completion for CNT-01..CNT-05 before any Flutter UI wiring.
Output: Content model, routes, controller behavior, and passing backend contract/cache tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-utility-content-platform/09-01-SUMMARY.md
@.planning/phases/09-utility-content-platform/09-CONTRACT-MATRIX.md
@backend/src/routes/index.js
@backend/src/middleware/validation.middleware.js
@backend/src/services/CacheService.js

<interfaces>
From backend/src/utils/ApiResponse.js:
javascript
sendSuccess(res, statusCode, message, data);

From backend/src/services/CacheService.js:
javascript
async get(key)
async set(key, value, ttl)
async delPattern(pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement versioned utility content domain and endpoint contracts</name>
  <files>backend/src/models/UtilityContent.model.js, backend/src/controllers/content.controller.js, backend/src/routes/content.routes.js, backend/src/routes/index.js, backend/src/middleware/validation.middleware.js</files>
  <behavior>
    - CNT-01/CNT-03/CNT-04: `/content/faqs`, `/content/bill-guide`, and `/content/legal/:slug` return published content with required metadata.
    - CNT-02: FAQ endpoint supports `q` and `topic` filtering with deterministic ordering.
    - API responses remain aligned to normalized success/error envelopes used across the backend.
  </behavior>
  <action>Create a `UtilityContent` model supporting kinds (`faq`, `bill_guide`, `legal`), slug, published status, version metadata, and payload fields required by each surface. Add content validation schemas in validation middleware for FAQ query params and legal slug params. Implement controller and route layer with strict contract behavior and mount under `/api/v1/content` in route index.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/content.contract.test.js</automated>
  </verify>
  <done>Content endpoints return contract-compliant payloads and deterministic filter behavior for CNT-01..CNT-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement cache-aware conditional refresh behavior for content endpoints</name>
  <files>backend/src/controllers/content.controller.js, backend/src/services/CacheService.js, backend/tests/content.cache.test.js</files>
  <behavior>
    - CNT-05: Responses emit `ETag` and honor `If-None-Match` with `304 Not Modified` when unchanged.
    - Changed content returns `200` with updated metadata and refreshed validator headers.
    - Cache key strategy prevents stale cross-surface responses.
  </behavior>
  <action>Compute stable response validators from content version/hash metadata, set `Cache-Control: no-cache`, and return 304 for matching validators. Use `CacheService` for read-through caching per content kind/slug/locale and invalidate keys when source content version changes. Keep behavior deterministic and aligned with tests from 09-01.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/content.cache.test.js tests/content.contract.test.js</automated>
  </verify>
  <done>Refresh behavior is validator-safe, deterministic, and proven by backend cache contract tests.</done>
</task>

</tasks>

<verification>
- Run backend content contract and cache tests in runInBand mode.
- Smoke-check endpoints with and without `If-None-Match` to confirm 200/304 split.
</verification>

<success_criteria>

- Backend `/api/v1/content` endpoints satisfy CNT-01..CNT-05 contracts.
- Filter/search and legal metadata behavior is deterministic.
- Conditional refresh semantics prevent stale-cache confusion.
  </success_criteria>

<output>
After completion, create `.planning/phases/09-utility-content-platform/09-02-SUMMARY.md`
</output>
