---
phase: 08-appliance-domain-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 08-03
files_modified:
  - wattwise_app/lib/feature/profile/screens/manage_appliances_screen.dart
  - wattwise_app/lib/feature/on_boarding/repository/appliance_repository.dart
  - wattwise_app/test/feature/profile/manage_appliances_retry_states_test.dart
  - wattwise_app/test/feature/profile/manage_appliances_delete_flow_test.dart
autonomous: true
requirements:
  - APP-01
  - APP-03
  - APP-04
gap_closure: true
must_haves:
  truths:
    - "Client create/patch payload fields match backend contract so validated mutations can complete deterministically."
    - "Delete requests satisfy backend precondition contract and no longer fail due to request-shape mismatch."
    - "Client expected-version extraction maps backend __v so stale-write handling receives a valid token path."
  artifacts:
    - path: "wattwise_app/lib/feature/profile/screens/manage_appliances_screen.dart"
      provides: "Manage Appliances payload normalization and expected-version extraction from backend payloads"
    - path: "wattwise_app/lib/feature/on_boarding/repository/appliance_repository.dart"
      provides: "DELETE precondition body mapping and shared expected-version token derivation including __v"
    - path: "wattwise_app/test/feature/profile/manage_appliances_delete_flow_test.dart"
      provides: "Regression coverage for delete precondition contract and rollback behavior"
    - path: "wattwise_app/test/feature/profile/manage_appliances_retry_states_test.dart"
      provides: "Regression coverage for stale-write token extraction and 412 recovery guidance"
  key_links:
    - from: "manage_appliances_screen.dart"
      to: "backend create/patch appliance schema"
      via: "usageHoursPerDay payload field mapping"
      pattern: "usageHoursPerDay"
    - from: "appliance_repository.dart"
      to: "backend DELETE /api/v1/appliances/:id validation"
      via: "request body _expectedVersion precondition"
      pattern: "_expectedVersion"
    - from: "appliance_repository.dart"
      to: "backend GET appliance payload"
      via: "version extraction fallback includes __v"
      pattern: "__v|expectedVersion"
---

<objective>
Close all Phase 8 verification blockers caused by client/backend mutation contract drift in Manage Appliances.

Purpose: Restore deterministic appliance mutation behavior by aligning request payload fields, delete preconditions, and stale-write version token mapping.
Output: Updated Flutter client contract mapping with targeted regression tests proving all listed blockers are resolved.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-appliance-domain-hardening/08-VERIFICATION.md
@.planning/phases/08-appliance-domain-hardening/08-03-PLAN.md
@.planning/phases/08-appliance-domain-hardening/08-03-SUMMARY.md
@wattwise_app/lib/feature/profile/screens/manage_appliances_screen.dart
@wattwise_app/lib/feature/on_boarding/repository/appliance_repository.dart
@wattwise_app/test/feature/profile/manage_appliances_retry_states_test.dart
@wattwise_app/test/feature/profile/manage_appliances_delete_flow_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for payload/delete/version contract mapping gaps</name>
  <files>wattwise_app/test/feature/profile/manage_appliances_retry_states_test.dart, wattwise_app/test/feature/profile/manage_appliances_delete_flow_test.dart</files>
  <behavior>
    - Create/patch payload assertions prove client sends usageHoursPerDay (not usageHours) per APP-01.
    - Delete flow assertions prove API request includes body._expectedVersion when token is available per APP-03.
    - Conflict-flow assertions prove __v from backend payload maps into expectedVersion token used for stale-write handling per APP-04.
  </behavior>
  <action>Extend existing Manage Appliances retry/delete tests to fail against current drift and explicitly validate request shape and version extraction rules from verification gaps. Keep coverage focused on the three blockers only; do not expand UI scope.</action>
  <verify>
    <automated>cd wattwise_app && flutter test test/feature/profile/manage_appliances_retry_states_test.dart test/feature/profile/manage_appliances_delete_flow_test.dart</automated>
  </verify>
  <done>Targeted tests fail before implementation and define the exact acceptance envelope for all three gap closures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement client contract alignment for create/patch/delete and stale-write tokens</name>
  <files>wattwise_app/lib/feature/profile/screens/manage_appliances_screen.dart, wattwise_app/lib/feature/on_boarding/repository/appliance_repository.dart</files>
  <behavior>
    - Outgoing create/patch payloads use backend-compatible usageHoursPerDay field naming.
    - Delete mutation sends body._expectedVersion precondition aligned with backend validation schema.
    - Expected-version extraction includes backend __v fallback so stale-write 412 handling remains deterministic.
  </behavior>
  <action>Update payload builder and mutation helpers to map usage hours to usageHoursPerDay, ensure delete requests include _expectedVersion in request body (while preserving current header behavior if present), and extend version extraction fallback chain to include __v from backend appliance payloads. Keep provider UX contract unchanged except for deterministic conflict/retry behavior now receiving valid version tokens.</action>
  <verify>
    <automated>cd wattwise_app && flutter test test/feature/profile/manage_appliances_retry_states_test.dart test/feature/profile/manage_appliances_delete_flow_test.dart</automated>
  </verify>
  <done>All three blockers are closed: payload contract mismatch resolved, delete precondition mismatch resolved, and __v stale-write token mapping resolved.</done>
</task>

</tasks>

<verification>
- Automated: `cd wattwise_app && flutter test test/feature/profile/manage_appliances_retry_states_test.dart test/feature/profile/manage_appliances_delete_flow_test.dart`
- Automated: `cd backend && npm test -- tests/appliance.concurrency.contract.test.js tests/appliance.validation.test.js`
- Manual sanity: perform add/edit/delete in Manage Appliances and confirm delete no longer rolls back from request-shape validation failures.
</verification>

<success_criteria>

- Verification gap 1 closed: client mutation payloads match backend contracts.
- Verification gap 2 closed: delete request precondition shape satisfies backend validation and avoids contract-mismatch rollback.
- Verification gap 3 closed: client expected-version extraction maps backend \_\_v for deterministic stale-write handling.
  </success_criteria>

<output>
After completion, create `.planning/phases/08-appliance-domain-hardening/08-04-SUMMARY.md`
</output>
