---
phase: 08-appliance-domain-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - backend/src/middleware/validation.middleware.js
  - backend/src/routes/appliance.routes.js
  - backend/src/controllers/appliance.controller.js
  - backend/src/models/Appliance.model.js
  - backend/tests/appliance.contract.test.js
  - backend/tests/appliance.non_destructive.test.js
  - backend/tests/appliance.concurrency.contract.test.js
  - backend/tests/appliance.validation.test.js
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-04
must_haves:
  truths:
    - "Create and update operations return deterministic success/error states with field-level validation details."
    - "Editing one appliance never removes unrelated active appliances owned by the same user."
    - "Stale-write updates are rejected with precondition conflict semantics instead of silent overwrite."
  artifacts:
    - path: "backend/src/middleware/validation.middleware.js"
      provides: "Schema set for create/patch/delete appliance contracts with deterministic details[] output"
    - path: "backend/src/controllers/appliance.controller.js"
      provides: "Non-destructive record mutation logic with optimistic concurrency checks"
    - path: "backend/tests/appliance.concurrency.contract.test.js"
      provides: "Regression guard against stale snapshot overwrite"
  key_links:
    - from: "backend/src/routes/appliance.routes.js"
      to: "backend/src/middleware/validation.middleware.js"
      via: "validate('createAppliance'|'patchAppliance'|'deleteAppliance')"
      pattern: "router.post|router.patch|router.delete"
    - from: "backend/src/controllers/appliance.controller.js"
      to: "backend/src/models/Appliance.model.js"
      via: "findOneAndUpdate filters include userId + isActive + version precondition"
      pattern: "__v|expectedVersion|isActive"
    - from: "backend/tests/appliance.non_destructive.test.js"
      to: "backend/src/controllers/appliance.controller.js"
      via: "assert unrelated records retained after single-item patch"
      pattern: "PATCH /api/v1/appliances/:id"
---

<objective>
Implement backend appliance mutation hardening with deterministic validation, non-destructive updates, and optimistic concurrency safety.

Purpose: Deliver API-level integrity guarantees required by APP-01, APP-02, and APP-04.
Output: Hardened appliance route/controller/model behavior and passing backend appliance contract suites.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-appliance-domain-hardening/08-RESEARCH.md
@.planning/phases/08-appliance-domain-hardening/08-VALIDATION.md
@.planning/phases/08-appliance-domain-hardening/08-CONTRACT-MATRIX.md
@.planning/phases/08-appliance-domain-hardening/08-MIGRATION-SAFETY.md
@.planning/phases/08-appliance-domain-hardening/08-01-SUMMARY.md
@backend/src/routes/appliance.routes.js
@backend/src/controllers/appliance.controller.js
@backend/src/middleware/validation.middleware.js
@backend/src/models/Appliance.model.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce validated per-resource mutation contracts (per APP-01 and APP-02)</name>
  <files>backend/src/middleware/validation.middleware.js, backend/src/routes/appliance.routes.js, backend/src/controllers/appliance.controller.js, backend/tests/appliance.contract.test.js, backend/tests/appliance.validation.test.js</files>
  <behavior>
    - POST create and PATCH update reject malformed fields using deterministic VALIDATION_ERROR envelopes.
    - PATCH updates only requested fields and preserves other active appliance records.
    - Compatibility `/bulk` path is retained only under migration-safety constraints and never used for ordinary single-item edits.
  </behavior>
  <action>Add dedicated validation schemas and route bindings for create/patch/delete appliance operations. Update controller create/update/delete handlers to follow deterministic success/error envelopes and to mutate per-resource records only. Keep ownership guards (`userId`) and active-state guards (`isActive`) on every mutation filter. Add regression assertions proving unrelated appliances remain intact after PATCH per APP-02. Keep `/bulk` available only as transitional compatibility path per migration runbook (no destructive replacement for manage-flow updates).</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/appliance.contract.test.js tests/appliance.validation.test.js tests/appliance.non_destructive.test.js</automated>
  </verify>
  <done>Create/update/delete contracts are validated and deterministic, and unrelated appliances are preserved during edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement optimistic concurrency preconditions and conflict recovery contract (per APP-04)</name>
  <files>backend/src/controllers/appliance.controller.js, backend/src/models/Appliance.model.js, backend/tests/appliance.concurrency.contract.test.js, backend/tests/appliance.contract.test.js</files>
  <behavior>
    - PATCH and DELETE require a version precondition and reject stale writes with conflict status.
    - Conflict response includes actionable message and normalized envelope fields for client recovery UX.
    - Successful updates return updated appliance payload including revision marker for next write.
  </behavior>
  <action>Implement optimistic concurrency in PATCH/DELETE using version-key preconditions (`__v` or equivalent revision token) and increment strategy on successful writes. Return 412 Precondition Failed for stale-write conflicts with deterministic payload fields (`success`, `message`, `errorCode`, `requestId`, `timestamp`, and optional `details`). Ensure response data includes updated revision to support safe subsequent writes. Extend backend tests to cover two-session stale snapshot scenario and verify no silent overwrite per APP-04.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/appliance.concurrency.contract.test.js tests/appliance.contract.test.js</automated>
  </verify>
  <done>Stale writes are blocked by precondition checks and conflict envelopes are stable for client recovery handling.</done>
</task>

</tasks>

<verification>
- Run backend appliance contract, validation, non-destructive, and concurrency tests.
- Confirm PATCH/DELETE stale-write behavior returns 412 and does not overwrite newer data.
- Confirm update flow does not deactivate or remove unrelated records.
</verification>

<success_criteria>

- Add/edit operations complete with deterministic success/error states.
- Updates are non-destructive and preserve unrelated appliance records.
- Concurrency-safe strategy prevents stale-write overwrite.
  </success_criteria>

<output>
After completion, create `.planning/phases/08-appliance-domain-hardening/08-02-SUMMARY.md`
</output>
