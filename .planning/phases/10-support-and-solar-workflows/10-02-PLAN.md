---
phase: 10-support-and-solar-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/controllers/solar.controller.js
  - backend/src/routes/solar.routes.js
  - backend/src/routes/index.js
  - backend/src/middleware/validation.middleware.js
  - backend/tests/solar.validation.contract.test.js
  - backend/tests/solar.range.contract.test.js
  - backend/tests/solar.recalculate.contract.test.js
  - backend/tests/solar.limitations.contract.test.js
autonomous: true
requirements:
  - SOL-01
  - SOL-02
  - SOL-03
  - SOL-04
must_haves:
  truths:
    - "User can submit required solar calculator inputs and get a valid estimate response."
    - "Estimate output is a low/base/high range with assumptions visible in payload."
    - "Input changes recompute deterministic updated ranges without stale values."
    - "Limitations/disclaimer text and confidence labels are always present."
  artifacts:
    - path: "backend/src/controllers/solar.controller.js"
      provides: "Stateless solar estimate computation endpoint"
      exports: ["calculateSolarEstimate"]
    - path: "backend/src/routes/solar.routes.js"
      provides: "Solar endpoint namespace"
      contains: "POST /estimate"
    - path: "backend/src/middleware/validation.middleware.js"
      provides: "Solar input schema"
      contains: "calculateSolarEstimate"
  key_links:
    - from: "backend/src/routes/solar.routes.js"
      to: "backend/src/controllers/solar.controller.js"
      via: "router.post('/estimate', validate('calculateSolarEstimate'), calculateSolarEstimate)"
      pattern: "router\\.post\('/estimate'.*calculateSolarEstimate"
    - from: "backend/src/controllers/solar.controller.js"
      to: "backend/src/utils/ApiResponse.js"
      via: "sendSuccess normalized envelope"
      pattern: "sendSuccess"
    - from: "backend/src/routes/index.js"
      to: "backend/src/routes/solar.routes.js"
      via: "router.use('/solar', solarRoutes)"
      pattern: "router\\.use\('/solar'"
---

<objective>
Implement backend Solar Calculator v1 contracts for transparent estimate ranges (SOL-01..SOL-04).

Purpose: Establish deterministic solar estimate API behavior before Flutter wiring.
Output: Solar controller/routes/validation and executable contract tests for range, recalculation, and limitations.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-support-and-solar-workflows/10-RESEARCH.md
@.planning/phases/10-support-and-solar-workflows/10-VALIDATION.md
@.planning/phases/09-utility-content-platform/09-02-SUMMARY.md

<interfaces>
From backend/src/middleware/validation.middleware.js:
- validate(schemaName, source = 'body') for route-level input binding.

From backend/src/utils/ApiResponse.js:

- sendSuccess(res, statusCode, message, data) for normalized success envelopes.

From backend/src/routes/index.js:

- Mount feature routes under /api/v1 with router.use('/feature', featureRoutes).
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED solar contract tests and validation schema for SOL-01..SOL-04</name>
  <files>backend/tests/solar.validation.contract.test.js, backend/tests/solar.range.contract.test.js, backend/tests/solar.recalculate.contract.test.js, backend/tests/solar.limitations.contract.test.js, backend/src/middleware/validation.middleware.js</files>
  <behavior>
    - Test 1: Missing monthlyUnits/roofArea/state/discom returns 400 VALIDATION_ERROR (SOL-01).
    - Test 2: Success payload includes low/base/high ranges and assumptions object (SOL-02).
    - Test 3: Distinct inputs produce deterministic recomputed outputs with no stale carry-over (SOL-03).
    - Test 4: limitations[] and informational disclaimer are always returned with confidenceLabel (SOL-04).
  </behavior>
  <action>Create focused backend contract tests and add calculateSolarEstimate schema with strict required inputs and bounded numeric constraints per research contract.</action>
  <verify>
    <automated>npm --prefix backend test -- --runInBand --runTestsByPath tests/solar.validation.contract.test.js tests/solar.range.contract.test.js tests/solar.recalculate.contract.test.js tests/solar.limitations.contract.test.js</automated>
  </verify>
  <done>Solar tests capture required range/assumption/disclaimer behavior and fail only due to missing endpoint implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement /api/v1/solar/estimate with transparent range output and limitations</name>
  <files>backend/src/controllers/solar.controller.js, backend/src/routes/solar.routes.js, backend/src/routes/index.js</files>
  <behavior>
    - Test 1: POST /solar/estimate returns 200 with recommendedSystemSizeKw and estimatedMonthlyGenerationKwh low/base/high.
    - Test 2: Response includes assumptions, limitations[], confidenceLabel, and disclaimer text.
    - Test 3: Input changes in monthlyUnits/roofArea/shading deterministically alter output ranges.
  </behavior>
  <action>Implement stateless estimate calculation that returns range-based outputs plus assumptions and limitations (not single-point precision), then mount solar routes in API index for SOL-01..SOL-04.</action>
  <verify>
    <automated>npm --prefix backend test -- --runInBand --runTestsByPath tests/solar.validation.contract.test.js tests/solar.range.contract.test.js tests/solar.recalculate.contract.test.js tests/solar.limitations.contract.test.js</automated>
  </verify>
  <done>All solar backend tests pass and endpoint contract is ready for Flutter integration.</done>
</task>

</tasks>

<verification>
Run targeted solar backend contracts, then API-level regression smoke:
- npm --prefix backend test -- --runInBand --runTestsByPath tests/solar.validation.contract.test.js tests/solar.range.contract.test.js tests/solar.recalculate.contract.test.js tests/solar.limitations.contract.test.js
- npm --prefix backend test -- --runInBand --runTestsByPath tests/sanity.test.js
</verification>

<success_criteria>
SOL-01..SOL-04 are satisfied server-side with strict input validation, transparent range outputs, deterministic recalculation behavior, and explicit limitations/disclaimer metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/10-support-and-solar-workflows/10-02-SUMMARY.md`
</output>
