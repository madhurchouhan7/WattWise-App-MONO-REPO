---
phase: 10-support-and-solar-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/SupportTicket.model.js
  - backend/src/controllers/support.controller.js
  - backend/src/routes/support.routes.js
  - backend/src/routes/index.js
  - backend/src/middleware/validation.middleware.js
  - backend/tests/support.contract.test.js
  - backend/tests/support.reference.contract.test.js
  - backend/tests/support.retry.contract.test.js
  - backend/tests/support.consent.audit.test.js
autonomous: true
requirements:
  - SUP-01
  - SUP-02
  - SUP-03
  - SUP-04
must_haves:
  truths:
    - "User can submit a support request with category, message, and contact details."
    - "Successful support submission returns a durable support reference ID."
    - "Failed support submission responses include actionable retry guidance."
    - "Support submissions persist legal consent and emit traceable metadata."
  artifacts:
    - path: "backend/src/models/SupportTicket.model.js"
      provides: "Durable ticket schema with immutable ticketRef and consent snapshot fields"
      contains: "ticketRef, consent.consentVersion, consent.acceptedAt"
    - path: "backend/src/controllers/support.controller.js"
      provides: "POST support ticket write path with normalized success/error envelopes"
      exports: ["submitSupportTicket"]
    - path: "backend/src/routes/support.routes.js"
      provides: "Support route namespace and validation bindings"
      contains: "POST /tickets"
    - path: "backend/src/middleware/validation.middleware.js"
      provides: "Zod support submission schema"
      contains: "createSupportTicket"
  key_links:
    - from: "backend/src/routes/support.routes.js"
      to: "backend/src/controllers/support.controller.js"
      via: "router.post('/tickets', validate('createSupportTicket'), submitSupportTicket)"
      pattern: "router\\.post\('/tickets'.*submitSupportTicket"
    - from: "backend/src/controllers/support.controller.js"
      to: "backend/src/models/SupportTicket.model.js"
      via: "SupportTicket.create"
      pattern: "SupportTicket\\.create"
    - from: "backend/src/controllers/support.controller.js"
      to: "backend/src/utils/ApiResponse.js"
      via: "sendSuccess/sendError normalized envelopes"
      pattern: "sendSuccess|sendError"
---

<objective>
Deliver backend support-ticket workflow contracts and persistence for SUP-01..SUP-04.

Purpose: Make support submission durable, traceable, and retry-safe before Flutter integration.
Output: Support ticket model, route/controller wiring, validation schema, and backend contract tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-support-and-solar-workflows/10-RESEARCH.md
@.planning/phases/10-support-and-solar-workflows/10-VALIDATION.md
@.planning/phases/09-utility-content-platform/09-02-SUMMARY.md

<interfaces>
From backend/src/utils/ApiResponse.js:
- sendSuccess(res, statusCode, message, data)
- sendError(res, statusCode, message)

From backend/src/middleware/validation.middleware.js:

- validate(schemaName, source = 'body')
- schemas object convention for route-bound validation contracts

From backend/src/routes/index.js:

- feature routes are mounted under /api/v1 via router.use('/feature', featureRoutes)
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add support contract tests and schema bindings for SUP-01, SUP-02, SUP-03, SUP-04</name>
  <files>backend/tests/support.contract.test.js, backend/tests/support.reference.contract.test.js, backend/tests/support.retry.contract.test.js, backend/tests/support.consent.audit.test.js, backend/src/middleware/validation.middleware.js</files>
  <behavior>
    - Test 1: Missing category/message/contact returns 400 VALIDATION_ERROR with details[] (SUP-01).
    - Test 2: Successful submit returns 201 with data.ticketRef and status OPEN (SUP-02).
    - Test 3: Retryable failures return deterministic errorCode and optional Retry-After guidance (SUP-03).
    - Test 4: Consent snapshot fields are persisted and trace metadata includes requestId/timestamp (SUP-04).
  </behavior>
  <action>Create RED-first backend tests for support endpoint contracts and add createSupportTicket Zod schema in validation middleware; keep envelope shape aligned with existing ApiResponse/error middleware conventions.</action>
  <verify>
    <automated>npm --prefix backend test -- --runInBand --runTestsByPath tests/support.contract.test.js tests/support.reference.contract.test.js tests/support.retry.contract.test.js tests/support.consent.audit.test.js</automated>
  </verify>
  <done>Support contract tests fail only for missing production wiring and pass schema-level assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement support model/controller/routes and mount under /api/v1/support</name>
  <files>backend/src/models/SupportTicket.model.js, backend/src/controllers/support.controller.js, backend/src/routes/support.routes.js, backend/src/routes/index.js</files>
  <behavior>
    - Test 1: POST /api/v1/support/tickets persists immutable ticketRef and returns normalized success envelope.
    - Test 2: Error paths preserve retry guidance semantics and include requestId.
    - Test 3: Consent snapshot is stored with policySlug, consentVersion, and acceptedAt.
  </behavior>
  <action>Implement durable ticket creation with server-generated ticketRef, status OPEN, consent snapshot persistence, and traceable metadata per SUP-01..SUP-04; wire support routes in index router and preserve existing API conventions.</action>
  <verify>
    <automated>npm --prefix backend test -- --runInBand --runTestsByPath tests/support.contract.test.js tests/support.reference.contract.test.js tests/support.retry.contract.test.js tests/support.consent.audit.test.js</automated>
  </verify>
  <done>All support backend tests pass and endpoint contract is stable for Flutter integration.</done>
</task>

</tasks>

<verification>
Run targeted support backend tests, then run backend sanity regression:
- npm --prefix backend test -- --runInBand --runTestsByPath tests/support.contract.test.js tests/support.reference.contract.test.js tests/support.retry.contract.test.js tests/support.consent.audit.test.js
- npm --prefix backend test -- --runInBand --runTestsByPath tests/sanity.test.js
</verification>

<success_criteria>
SUP-01..SUP-04 are satisfied server-side with deterministic request validation, durable ticket references, actionable retry semantics, and consent traceability persisted in the support domain model.
</success_criteria>

<output>
After completion, create `.planning/phases/10-support-and-solar-workflows/10-01-SUMMARY.md`
</output>
