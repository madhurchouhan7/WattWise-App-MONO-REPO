---
phase: 07-contract-freeze-and-profile-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-contract-freeze-and-profile-wiring/07-CONTRACT-MATRIX.md
  - wattwise_app/lib/core/network/api_constants.dart
  - wattwise_app/lib/feature/profile/screens/profile_screen.dart
  - wattwise_app/lib/feature/profile/screens/edit_profile_screen.dart
  - backend/tests/profile.contract.test.js
  - backend/tests/profile.validation.test.js
  - wattwise_app/test/feature/profile/profile_load_states_test.dart
  - wattwise_app/test/feature/profile/profile_validation_test.dart
  - wattwise_app/test/feature/profile/profile_persistence_test.dart
autonomous: true
requirements:
  - PRO-01
must_haves:
  truths:
    - "Profile and settings endpoint contracts are frozen in a single matrix for load/save paths before implementation drift can occur."
    - "Profile action rows no longer dead-end; Edit Profile routes to a functional screen shell that can host real provider-backed UX."
    - "Wave-0 profile test files exist so subsequent plans can run automated checks without missing-file blockers."
  artifacts:
    - path: ".planning/phases/07-contract-freeze-and-profile-wiring/07-CONTRACT-MATRIX.md"
      provides: "Canonical GET/PUT profile and settings contract table with success/error envelopes and retry semantics"
    - path: "wattwise_app/lib/feature/profile/screens/profile_screen.dart"
      provides: "Functional navigation from profile actions to edit flow (no placeholder onTap)"
    - path: "backend/tests/profile.contract.test.js"
      provides: "Wave-0 backend contract test scaffold for /api/v1/users/me"
    - path: "wattwise_app/test/feature/profile/profile_load_states_test.dart"
      provides: "Wave-0 Flutter load/error/retry state test scaffold"
  key_links:
    - from: "profile_screen.dart"
      to: "edit_profile_screen.dart"
      via: "Navigator push route"
      pattern: "Edit Profile|Navigator"
    - from: "api_constants.dart"
      to: "auth_repository.dart|profile repository"
      via: "single endpoint constant"
      pattern: "users/me"
    - from: "07-CONTRACT-MATRIX.md"
      to: "backend tests + Flutter provider implementation"
      via: "frozen request/response/error contract"
      pattern: "GET /api/v1/users/me|PUT /api/v1/users/me"
---

<objective>
Freeze profile/settings contracts and remove placeholder profile navigation so Phase 7 implementation starts from a stable endpoint matrix and executable UI entry flow.

Purpose: Prevent contract drift and unblock deterministic PRO-01 implementation.
Output: Contract matrix, functional Edit Profile navigation shell, and Wave-0 test scaffolding for backend + Flutter profile paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-RESEARCH.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-VALIDATION.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-UI-SPEC.md
@wattwise_app/lib/feature/profile/screens/profile_screen.dart
@wattwise_app/lib/core/network/api_constants.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author frozen endpoint contract matrix for profile/settings load/save paths</name>
  <files>.planning/phases/07-contract-freeze-and-profile-wiring/07-CONTRACT-MATRIX.md</files>
  <action>Create a single contract matrix covering `GET /api/v1/users/me` and `PUT /api/v1/users/me` plus any profile-settings read/write companion endpoints already used by the app. For each endpoint, define request fields, response envelope, error envelope, field mapping (`name/avatarUrl` ↔ display fields), retry semantics, and persistence notes per PRO-01. Explicitly lock one PUT strategy (always return updated profile payload) and record that this is required for later persistence verification.</action>
  <verify>
    <automated>rg -n "GET /api/v1/users/me|PUT /api/v1/users/me|error envelope|retry" .planning/phases/07-contract-freeze-and-profile-wiring/07-CONTRACT-MATRIX.md</automated>
  </verify>
  <done>Contract matrix exists and contains frozen load/save contracts with explicit success/error/retry behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace placeholder profile action with functional edit-flow navigation shell</name>
  <files>wattwise_app/lib/feature/profile/screens/profile_screen.dart, wattwise_app/lib/feature/profile/screens/edit_profile_screen.dart, wattwise_app/lib/core/network/api_constants.dart</files>
  <behavior>
    - Edit Profile action is navigable and no longer uses a placeholder callback.
    - Endpoint constants are normalized to `/users/me` to eliminate stale path drift.
    - Screen shell applies UI-SPEC base constraints: heading/call-to-action copy, spacing tokens (4/8/16/24), and typography weights limited to 400/600.
  </behavior>
  <action>Implement route wiring from Profile screen action tile to a concrete `EditProfileScreen` shell. Remove any `onTap: () {}` placeholder for edit action and keep other actions unchanged. Normalize profile constant usage to `/users/me` in API constants so data-layer tasks inherit a single path. In the edit shell, include the UI-SPEC primary CTA copy `Save Profile` and reserve accent treatment for primary action and retry affordances only.</action>
  <verify>
    <automated>cd wattwise_app && flutter analyze lib/feature/profile/screens/profile_screen.dart lib/feature/profile/screens/edit_profile_screen.dart lib/core/network/api_constants.dart</automated>
  </verify>
  <done>Profile action route is functional, stale constant drift is removed, and edit-flow shell follows UI-SPEC baseline constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Wave-0 profile test scaffolding for backend and Flutter</name>
  <files>backend/tests/profile.contract.test.js, backend/tests/profile.validation.test.js, wattwise_app/test/feature/profile/profile_load_states_test.dart, wattwise_app/test/feature/profile/profile_validation_test.dart, wattwise_app/test/feature/profile/profile_persistence_test.dart</files>
  <behavior>
    - Missing profile test files are created so no later task has `MISSING` automated verification blockers.
    - Scaffolded tests encode expected behaviors from PRO-01..PRO-04 with TODO markers only where later plans implement details.
  </behavior>
  <action>Create minimal runnable test files listed in Wave-0 section of 07-VALIDATION.md. Each file must compile and include at least one failing-or-pending expectation tied to the frozen contract and UI behavior targets. Keep tests deterministic and focused on profile domain only.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/profile.contract.test.js tests/profile.validation.test.js && cd ../wattwise_app && flutter test test/feature/profile/profile_load_states_test.dart test/feature/profile/profile_validation_test.dart test/feature/profile/profile_persistence_test.dart</automated>
  </verify>
  <done>Wave-0 profile test files exist and run, enabling strict automated verification in Plans 07-02 and 07-03.</done>
</task>

</tasks>

<verification>
- Confirm contract matrix includes load/save path contracts, envelopes, and retry semantics.
- Run `cd wattwise_app && flutter analyze lib/feature/profile/screens/profile_screen.dart lib/feature/profile/screens/edit_profile_screen.dart lib/core/network/api_constants.dart`.
- Run profile scaffold commands in backend and Flutter to ensure verification paths are no longer blocked by missing files.
</verification>

<success_criteria>
- Endpoint contract matrix exists for profile/settings load and save paths.
- Edit Profile action navigates to a functional flow shell (no placeholder callback).
- Wave-0 profile test scaffolding exists for all required profile checks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-contract-freeze-and-profile-wiring/07-01-SUMMARY.md`
</output>
