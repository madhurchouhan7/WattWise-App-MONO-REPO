---
phase: 07-contract-freeze-and-profile-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/src/routes/user.routes.js
  - backend/src/middleware/validation.middleware.js
  - backend/src/controllers/user.controller.js
  - backend/src/services/UserService.js
  - backend/src/models/User.js
  - backend/tests/profile.contract.test.js
  - backend/tests/profile.validation.test.js
autonomous: true
requirements:
  - PRO-02
  - PRO-03
must_haves:
  truths:
    - "Profile edit requests validate deterministically before controller logic and return normalized error envelopes for field-level feedback."
    - "Successful profile updates return a consistent updated-profile payload to support immediate UI reconciliation."
    - "Invalid input paths produce machine-usable field error details that can be surfaced inline on Flutter forms."
  artifacts:
    - path: "backend/src/routes/user.routes.js"
      provides: "Route-level validation wiring for PUT /api/v1/users/me"
    - path: "backend/src/middleware/validation.middleware.js"
      provides: "Frozen profile update schema aligned to editable fields and constraints"
    - path: "backend/tests/profile.validation.test.js"
      provides: "Automated negative-path checks for inline validation error mapping"
  key_links:
    - from: "user.routes.js"
      to: "validation.middleware.js"
      via: "validate('updateProfile') middleware"
      pattern: "PUT|updateProfile"
    - from: "user.controller.js"
      to: "UserService.updateProfile"
      via: "normalized response envelope"
      pattern: "ApiResponse|success"
    - from: "error handler envelope"
      to: "Flutter inline form errors"
      via: "details[] field mapping"
      pattern: "details|path|message"
---

<objective>
Implement and enforce the frozen backend profile update contract so profile editing and inline validation behavior are deterministic and testable.

Purpose: Deliver PRO-02 and PRO-03 through a single validated backend contract consumed by Flutter forms.
Output: Route validation wiring, normalized update response, aligned model/schema constraints, and passing backend profile contract/validation tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-RESEARCH.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-VALIDATION.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-CONTRACT-MATRIX.md
@.planning/phases/07-contract-freeze-and-profile-wiring/07-01-SUMMARY.md
@backend/src/routes/user.routes.js
@backend/src/middleware/validation.middleware.js
@backend/src/controllers/user.controller.js
@backend/src/services/UserService.js
@backend/src/models/User.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce route-bound validation and normalize update response contract</name>
  <files>backend/src/routes/user.routes.js, backend/src/middleware/validation.middleware.js, backend/src/controllers/user.controller.js, backend/src/services/UserService.js, backend/src/models/User.js</files>
  <behavior>
    - PUT `/api/v1/users/me` always runs schema validation before controller execution.
    - Response shape for successful update is stable and includes updated profile data.
    - Validation rules are aligned across schema and model (no min-length mismatch drift).
  </behavior>
  <action>Attach `validate('updateProfile')` middleware to `PUT /users/me` route. Update schema constraints and model validators to one canonical rule set for editable fields in scope (`name`, `avatarUrl`, and supported equivalent fields). Ensure controller/service always return the updated profile payload using normalized success envelope (do not branch into mixed ack-only behavior). Keep endpoint path and auth middleware behavior unchanged.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/profile.contract.test.js</automated>
  </verify>
  <done>Profile update path is contract-consistent, validated at route boundary, and returns updated payload deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit field-level validation error details for inline Flutter mapping</name>
  <files>backend/src/middleware/validation.middleware.js, backend/src/controllers/user.controller.js, backend/tests/profile.validation.test.js</files>
  <behavior>
    - Invalid payloads return normalized error envelope plus field-level details list.
    - Error detail format is stable enough for direct form field mapping in Flutter.
    - Retryable server failures remain distinguishable from validation failures.
  </behavior>
  <action>Harden validation/error path so 4xx validation failures include deterministic `details[]` entries containing field path and human-readable message; keep top-level envelope compatible with existing error middleware. Extend backend validation tests to cover empty/short name, malformed avatar URL, and unsupported field payloads. Ensure tests assert both envelope fields and detail payload shape.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/profile.validation.test.js tests/profile.contract.test.js</automated>
  </verify>
  <done>Backend returns inline-mappable field errors and contract/validation suites pass for profile update paths.</done>
</task>

</tasks>

<verification>
- Run `cd backend && npm test -- --runInBand tests/profile.contract.test.js tests/profile.validation.test.js`.
- Confirm update success returns updated profile payload in normalized envelope.
- Confirm validation failures include deterministic `details[]` usable by Flutter inline form mapping.
</verification>

<success_criteria>
- Profile fields can be edited and saved successfully through stable backend contract.
- Invalid profile inputs generate inline-mappable validation errors before submission succeeds.
- Backend contract tests for `/api/v1/users/me` GET/PUT pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-contract-freeze-and-profile-wiring/07-02-SUMMARY.md`
</output>
