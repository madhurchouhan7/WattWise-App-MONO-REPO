---
phase: 02-compatibility-foundation-and-dual-path-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/efficiency_plan/orchestrators/modeResolver.js
  - backend/src/agents/efficiency_plan/collaborative.index.js
autonomous: true
requirements:
  - COMP-02
must_haves:
  truths:
    - "Routing mode is resolved deterministically from x-ai-mode header or environment default."
    - "Collaborative path exists as an additive entrypoint without modifying legacy graph exports."
  artifacts:
    - "backend/src/agents/efficiency_plan/orchestrators/modeResolver.js"
    - "backend/src/agents/efficiency_plan/collaborative.index.js"
  key_links:
    - "resolveOrchestrationMode(req) returns requestedMode and executionPath for controller dispatch."
    - "collaborativePlanApp exports invoke-compatible interface matching legacy invoke contract."
---

<objective>
Create the dual-path foundation contracts needed for safe controller routing in Phase 2.

Purpose: Establish strict mode resolution and an additive collaborative entrypoint before modifying API behavior.
Output: A reusable mode resolver and collaborative invoke entrypoint that can be wired in later plans.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-CONTEXT.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-RESEARCH.md
@backend/src/controllers/ai.controller.js
@backend/src/agents/efficiency_plan/index.js
@backend/src/utils/ApiError.js

<interfaces>
From backend/src/agents/efficiency_plan/index.js:
```javascript
const efficiencyPlanApp = workflow.compile();
module.exports = { efficiencyPlanApp };
```

From backend/src/controllers/ai.controller.js:
```javascript
const resultState = await efficiencyPlanApp.invoke(initialState);
const plan = resultState.finalPlan;
```

From backend/src/utils/ApiError.js:
```javascript
class ApiError extends Error {
  constructor(statusCode, message = "Something went wrong")
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict orchestration mode resolver contract</name>
  <read_first>backend/src/controllers/ai.controller.js, backend/src/utils/ApiError.js, .planning/phases/02-compatibility-foundation-and-dual-path-routing/02-CONTEXT.md</read_first>
  <files>backend/src/agents/efficiency_plan/orchestrators/modeResolver.js</files>
  <behavior>
    - Returns requestedMode=legacy and executionPath=legacy when NODE_ENV=production and x-ai-mode is absent.
    - Returns requestedMode=collaborative and executionPath=collaborative when NODE_ENV is not production and x-ai-mode is absent.
    - Throws ApiError(400) for unknown x-ai-mode values with allowed values listed as "legacy, collaborative".
    - Throws ApiError(400) when any future mode fields are present outside the header (body.aiMode, body.mode, query.aiMode, query.mode), per conflict policy.
  </behavior>
  <action>Create resolveOrchestrationMode(req) in backend/src/agents/efficiency_plan/orchestrators/modeResolver.js. Enforce D-01, D-02, D-03, and D-04 exactly: default mode uses NODE_ENV (production=>legacy, otherwise collaborative), override source is only x-ai-mode header, and invalid/conflicting inputs fail with status 400. Return an object with requestedMode and executionPath set to the selected enum value.</action>
  <verify>
    <automated>cd backend && node -e "const {resolveOrchestrationMode}=require('./src/agents/efficiency_plan/orchestrators/modeResolver'); const req={get:(k)=>undefined,body:{},query:{}}; const out=resolveOrchestrationMode(req,{nodeEnv:'production'}); if(out.requestedMode!=='legacy'||out.executionPath!=='legacy') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Resolver accepts only legacy or collaborative; produces deterministic defaults by environment; and emits explicit 400 ApiError for invalid/conflicting mode inputs with no silent coercion.</acceptance_criteria>
  <done>Mode resolution contract is ready for controller integration and fully aligned to D-01..D-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add additive collaborative entrypoint with invoke-compatible shape</name>
  <read_first>backend/src/agents/efficiency_plan/index.js, backend/src/agents/efficiency_plan/state.js, .planning/phases/02-compatibility-foundation-and-dual-path-routing/02-CONTEXT.md</read_first>
  <files>backend/src/agents/efficiency_plan/collaborative.index.js</files>
  <behavior>
    - Exports collaborativePlanApp with an invoke(initialState) method.
    - invoke(...) resolves to an object containing finalPlan for compatibility.
    - No edits are made to legacy efficiencyPlanApp exports.
  </behavior>
  <action>Create backend/src/agents/efficiency_plan/collaborative.index.js as a distinct additive module (D-05). Implement an invoke-compatible app export named collaborativePlanApp. Keep implementation minimal for Phase 2, but return state containing finalPlan and metadata-compatible fields so controller can wrap them later. Do not import or mutate legacy workflow internals in backend/src/agents/efficiency_plan/index.js (COMP-01 boundary).</action>
  <verify>
    <automated>cd backend && node -e "const {collaborativePlanApp}=require('./src/agents/efficiency_plan/collaborative.index'); if(!collaborativePlanApp||typeof collaborativePlanApp.invoke!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Collaborative module exists as isolated entrypoint, exports invoke(), and can be selected by router without changing legacy entrypoint behavior.</acceptance_criteria>
  <done>Collaborative execution path is technically available and isolated for explicit routing.</done>
</task>

</tasks>

<verification>
- Confirm resolver rejects invalid/conflicting mode inputs with 400 status and no fallback.
- Confirm collaborative entrypoint exports invoke-compatible contract.
- Confirm legacy entrypoint file remains unchanged and callable.
</verification>

<success_criteria>
- Requested mode can be resolved deterministically for all supported inputs.
- Collaborative entrypoint exists and is callable independently from legacy path.
- No legacy graph export regression introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-01-SUMMARY.md`
</output>
