---
phase: 02-compatibility-foundation-and-dual-path-routing
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/src/controllers/ai.controller.js
  - backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js
autonomous: true
requirements:
  - COMP-01
  - COMP-02
must_haves:
  truths:
    - "API route keeps legacy success behavior while exposing selected execution path metadata."
    - "Mode selection is transparent in success responses via executionPath and requestedMode."
    - "finalPlan remains stable for existing clients."
  artifacts:
    - "backend/src/controllers/ai.controller.js"
    - "backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js"
  key_links:
    - "Controller uses resolveOrchestrationMode(...) to select efficiencyPlanApp or collaborativePlanApp."
    - "Controller wraps output with metadata keys: executionPath, requestedMode, requestId, orchestrationVersion, qualityScore, debateRounds."
---

<objective>
Integrate dual-path routing into the AI controller while preserving backward compatibility of the successful payload contract.

Purpose: Make collaborative mode selectable without breaking existing clients that depend on finalPlan semantics.
Output: Mode-aware controller dispatch + compatibility envelope helper with required metadata keys.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-CONTEXT.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-RESEARCH.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-01-PLAN.md
@backend/src/controllers/ai.controller.js
@backend/src/utils/ApiResponse.js
@backend/src/app.js

<interfaces>
From backend/src/utils/ApiResponse.js:
```javascript
const sendSuccess = (res, statusCode = 200, message = 'Success', data = null) => {
  const payload = { success: true, message };
  if (data !== null) payload.data = data;
  return res.status(statusCode).json(payload);
};
```

Required success metadata keys from locked decisions (D-12):
```text
executionPath
requestedMode
requestId
orchestrationVersion
qualityScore
debateRounds
```

From backend/src/app.js:
```javascript
req.id = uuidv4();
res.setHeader('X-Request-ID', req.id);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create compatibility envelope adapter for successful responses</name>
  <read_first>backend/src/utils/ApiResponse.js, backend/src/controllers/ai.controller.js, .planning/phases/02-compatibility-foundation-and-dual-path-routing/02-CONTEXT.md</read_first>
  <files>backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js</files>
  <behavior>
    - Adapter returns object containing finalPlan unchanged at its contract location.
    - Metadata includes exactly the required keys from D-12.
    - orchestrationVersion is set to literal value v2-phase2 per D-13.
  </behavior>
  <action>Implement buildPlanResponseEnvelope({finalPlan, requestedMode, executionPath, requestId, qualityScore, debateRounds}) in backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js. Preserve D-10 and D-11 by keeping finalPlan compatibility while placing metadata outside finalPlan. Set metadata.orchestrationVersion to v2-phase2 and default qualityScore=null, debateRounds=0 when unavailable.</action>
  <verify>
    <automated>cd backend && node -e "const {buildPlanResponseEnvelope}=require('./src/agents/efficiency_plan/orchestrators/responseEnvelope'); const out=buildPlanResponseEnvelope({finalPlan:{steps:[]},requestedMode:'legacy',executionPath:'legacy',requestId:'r1'}); const k=Object.keys(out.metadata).sort().join(','); if(k!=='debateRounds,executionPath,orchestrationVersion,qualityScore,requestId,requestedMode') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Envelope helper preserves finalPlan compatibility and always emits required metadata keys with deterministic defaults.</acceptance_criteria>
  <done>Controller can safely return one compatibility-safe response shape for both execution paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire mode resolver + dual dispatch into AI controller</name>
  <read_first>backend/src/controllers/ai.controller.js, backend/src/agents/efficiency_plan/orchestrators/modeResolver.js, backend/src/agents/efficiency_plan/collaborative.index.js, backend/src/agents/efficiency_plan/index.js</read_first>
  <files>backend/src/controllers/ai.controller.js</files>
  <behavior>
    - Legacy mode selects efficiencyPlanApp.invoke(initialState).
    - Collaborative mode selects collaborativePlanApp.invoke(initialState).
    - Successful response includes executionPath metadata and finalPlan compatibility.
  </behavior>
  <action>Update getEfficiencyPlan in backend/src/controllers/ai.controller.js to call resolveOrchestrationMode(req), select execution app by strict enum (D-03), and dispatch exactly one path (D-06). Build response through buildPlanResponseEnvelope and send via sendSuccess. Preserve existing weather-context behavior and existing input validation, and keep finalPlan contract backward compatible (D-10, D-14). Do not add query/body mode routing in this phase (D-02, D-04).</action>
  <verify>
    <automated>cd backend && node -e "const c=require('./src/controllers/ai.controller'); if(typeof c.getEfficiencyPlan!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Controller performs deterministic single-path dispatch, returns required metadata keys, and preserves legacy finalPlan compatibility.</acceptance_criteria>
  <done>Dual-path routing is active and transparent while legacy consumers remain functional.</done>
</task>

</tasks>

<verification>
- Validate controller dispatches exactly one execution path and reports executionPath in success metadata.
- Validate metadata contains all required keys and orchestrationVersion=v2-phase2.
- Validate finalPlan compatibility for legacy consumer expectations.
</verification>

<success_criteria>
- Legacy and collaborative modes are both callable through the same endpoint.
- Success payload is API-compatible with additive metadata only.
- Routing transparency is visible in every successful response.
</success_criteria>

<output>
After completion, create `.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-02-SUMMARY.md`
</output>
