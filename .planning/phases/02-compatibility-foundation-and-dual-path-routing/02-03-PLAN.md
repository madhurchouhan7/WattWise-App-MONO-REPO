---
phase: 02-compatibility-foundation-and-dual-path-routing
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - backend/package.json
  - backend/tests/helpers/mockOrchestrators.js
  - backend/tests/ai.compat.legacy.test.js
  - backend/tests/ai.compat.routing.test.js
  - backend/tests/ai.compat.errors.test.js
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - OPS-03
must_haves:
  truths:
    - "Legacy route behavior is regression-tested and remains callable."
    - "Mode routing rules are validated for environment defaults and header overrides."
    - "Collaborative failures return centralized structured errors including errorCode and requestId without process crash."
  artifacts:
    - "backend/tests/ai.compat.legacy.test.js"
    - "backend/tests/ai.compat.routing.test.js"
    - "backend/tests/ai.compat.errors.test.js"
    - "backend/tests/helpers/mockOrchestrators.js"
  key_links:
    - "Route tests call /api/v1/ai/generate-plan and assert finalPlan + metadata compatibility."
    - "Error-path test asserts centralized middleware fields: success=false, errorCode, requestId."
---

<objective>
Lock Phase 2 compatibility behavior with automated integration tests and structured error assertions.

Purpose: Prove COMP-01, COMP-02, and OPS-03 with repeatable commands before Phase 3 work begins.
Output: Route-level test matrix for legacy parity, mode routing, and centralized error behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-VALIDATION.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-RESEARCH.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-02-PLAN.md
@backend/src/controllers/ai.controller.js
@backend/src/middleware/errorHandler.js
@backend/src/routes/ai.routes.js
@backend/src/app.js

<interfaces>
From backend/src/routes/ai.routes.js:
```javascript
router.post('/generate-plan', authMiddleware, getEfficiencyPlan);
```

From backend/src/middleware/errorHandler.js response shape:
```javascript
{
  success: false,
  message,
  errorCode,
  timestamp,
  requestId
}
```

Phase 2 success contract target:
```text
success=true
message='Plan generated successfully.'
data.finalPlan (compatible)
data.metadata.executionPath/requestedMode/requestId/orchestrationVersion/qualityScore/debateRounds
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic test harness and orchestrator stubs</name>
  <read_first>backend/package.json, backend/tests/sanity.test.js, backend/src/routes/ai.routes.js</read_first>
  <files>backend/package.json, backend/tests/helpers/mockOrchestrators.js</files>
  <behavior>
    - Supertest is available for HTTP-level assertions.
    - Mock helper exports legacy success, collaborative success, and collaborative error stubs.
  </behavior>
  <action>Install supertest as backend dev dependency at version ^7.2.2 and create backend/tests/helpers/mockOrchestrators.js with explicit stub factories: legacySuccessStub returns finalPlan, collaborativeSuccessStub returns finalPlan + optional qualityScore/debateRounds, collaborativeFailureStub throws ApiError(500, 'Collaborative path failed'). Keep mocks deterministic and side-effect free.</action>
  <verify>
    <automated>cd backend && npm ls supertest && node -e "const m=require('./tests/helpers/mockOrchestrators'); if(typeof m.legacySuccessStub!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Test harness dependencies and shared orchestrator mocks are present and reusable by all Phase 2 compatibility tests.</acceptance_criteria>
  <done>Test foundation is in place for route-level compatibility matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement legacy and routing matrix integration tests</name>
  <read_first>backend/src/controllers/ai.controller.js, backend/src/app.js, backend/tests/helpers/mockOrchestrators.js</read_first>
  <files>backend/tests/ai.compat.legacy.test.js, backend/tests/ai.compat.routing.test.js</files>
  <behavior>
    - Legacy-mode test proves finalPlan compatibility and executionPath=legacy when default or explicit legacy.
    - Routing test proves non-production default is collaborative and x-ai-mode header override is respected.
    - Invalid x-ai-mode returns HTTP 400 with allowed values list.
  </behavior>
  <action>Create backend/tests/ai.compat.legacy.test.js and backend/tests/ai.compat.routing.test.js using Jest + Supertest against /api/v1/ai/generate-plan with auth middleware mocked as pass-through. Include table-driven cases for NODE_ENV=production default legacy, NODE_ENV=development default collaborative, explicit x-ai-mode overrides, and invalid header values. Assert response fields: success, data.finalPlan presence, metadata.executionPath, metadata.requestedMode, metadata.orchestrationVersion==='v2-phase2'.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/ai.compat.legacy.test.js tests/ai.compat.routing.test.js</automated>
  </verify>
  <acceptance_criteria>Legacy compatibility and mode-routing behavior are covered by deterministic route tests with explicit assertions for required metadata keys.</acceptance_criteria>
  <done>COMP-01 and COMP-02 are enforced by automated tests at endpoint level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement centralized error-path integration test (no fallback)</name>
  <read_first>backend/src/middleware/errorHandler.js, backend/src/controllers/ai.controller.js, backend/tests/helpers/mockOrchestrators.js</read_first>
  <files>backend/tests/ai.compat.errors.test.js</files>
  <behavior>
    - Collaborative execution failure does not run legacy fallback.
    - Error response is centralized shape with success=false, errorCode, requestId.
    - Process remains alive and test run continues after failure assertion.
  </behavior>
  <action>Create backend/tests/ai.compat.errors.test.js to force collaborative stub failure and assert HTTP 500 response from centralized middleware includes errorCode and requestId (D-08). Add assertion that legacy stub invoke was not called during collaborative failure (D-07). Include one invalid-mode case asserting HTTP 400 with structured error payload. Ensure tests never assert or require automatic fallback (D-09).</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/ai.compat.errors.test.js && npm test -- --runInBand tests/ai.compat.legacy.test.js tests/ai.compat.routing.test.js tests/ai.compat.errors.test.js</automated>
  </verify>
  <acceptance_criteria>Collaborative failure path is proven recoverable through centralized error handling with no automatic fallback and no process crash.</acceptance_criteria>
  <done>OPS-03 is validated by automated integration test coverage for centralized recoverable failures.</done>
</task>

</tasks>

<verification>
- Run Phase 2 compatibility suite: `cd backend && npm test -- --runInBand tests/ai.compat.legacy.test.js tests/ai.compat.routing.test.js tests/ai.compat.errors.test.js`.
- Confirm all tests pass and cover default mode rules, override behavior, and centralized error contract.
- Confirm no test scenario allows or expects automatic fallback from collaborative to legacy.
</verification>

<success_criteria>
- Legacy behavior parity is demonstrated by route-level regression tests.
- Routing contract is deterministic and fully tested for defaults and overrides.
- Structured recoverable error behavior is validated for collaborative failures.
</success_criteria>

<output>
After completion, create `.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-03-SUMMARY.md`
</output>
