---
phase: 03-shared-memory-and-context-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/agents/efficiency_plan/shared/memoryKeys.js
  - backend/src/agents/efficiency_plan/shared/memorySchema.js
  - backend/src/agents/efficiency_plan/shared/redaction.js
  - backend/tests/helpers/memoryFixtures.js
  - backend/tests/memory.provenance.schema.test.js
autonomous: true
requirements:
  - MEM-03
  - OPS-02
must_haves:
  truths:
    - "Memory write payloads are validated against a strict provenance contract before persistence."
    - "Missing tenantId, userId, or threadId is rejected with deterministic 400-ready validation errors."
    - "Sensitive fields are redacted/tokenized before persistence payload assembly."
  artifacts:
    - path: "backend/src/agents/efficiency_plan/shared/memoryKeys.js"
      provides: "Canonical tenant:user:thread key builder and identity validation"
    - path: "backend/src/agents/efficiency_plan/shared/memorySchema.js"
      provides: "Zod schemas for write/read/provenance contracts"
    - path: "backend/src/agents/efficiency_plan/shared/redaction.js"
      provides: "Sensitive-field sanitization for memory events"
    - path: "backend/tests/memory.provenance.schema.test.js"
      provides: "Automated schema and provenance guard assertions"
  key_links:
    - from: "memoryKeys.assertMemoryIdentity"
      to: "memoryService.writeEvent"
      via: "pre-write guard"
      pattern: "assertMemoryIdentity\("
    - from: "memorySchema.MemoryEventSchema"
      to: "memoryService.writeEvent"
      via: "safeParse enforcement"
      pattern: "MemoryEventSchema\.safeParse"
    - from: "redaction.redactMemoryPayload"
      to: "memoryService.writeEvent"
      via: "payload sanitization"
      pattern: "redactMemoryPayload\("
---

<objective>
Create Phase 3 foundation contracts for identity scoping, provenance validation, and redaction so downstream memory services can be implemented without ambiguity.

Purpose: Enforce locked decision invariants (D-02, D-04, D-08, D-09, D-11, D-12) before any persistence wiring.
Output: Shared contract modules and deterministic tests proving provenance and identity validation behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-RESEARCH.md
@.planning/phases/02-compatibility-foundation-and-dual-path-routing/02-03-SUMMARY.md
@backend/src/utils/ApiError.js
@backend/src/middleware/errorHandler.js
@backend/src/agents/efficiency_plan/orchestrators/modeResolver.js

<interfaces>
From backend/src/agents/efficiency_plan/orchestrators/modeResolver.js:
```javascript
function resolveOrchestrationMode(req, options = {}) {
  return { requestedMode: selectedMode, executionPath: selectedMode };
}
```

From backend/src/middleware/errorHandler.js:

```javascript
const response = {
  success: false,
  message,
  errorCode,
  timestamp: new Date().toISOString(),
  requestId: req.id || generateRequestId(),
};
```

Locked provenance fields (D-09):

```text
agentId, timestamp, sourceType, evidenceRefs, revisionId, confidenceScore
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement identity key builder and strict provenance schemas</name>
  <read_first>backend/src/utils/ApiError.js, .planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md, .planning/phases/03-shared-memory-and-context-infrastructure/03-RESEARCH.md</read_first>
  <files>backend/src/agents/efficiency_plan/shared/memoryKeys.js, backend/src/agents/efficiency_plan/shared/memorySchema.js</files>
  <behavior>
    - Identity contract requires non-empty tenantId, userId, threadId and rejects missing keys (D-02, D-04).
    - MemoryEventSchema requires agentId, timestamp, sourceType, revisionId, confidenceScore, and trace linkage fields when provided (D-09, D-12).
    - evidenceRefs may be empty only when confidenceScore is downgraded to <=0.49 and noEvidenceReason is set (D-11 explicit allowance).
  </behavior>
  <action>Create backend/src/agents/efficiency_plan/shared/memoryKeys.js with buildThreadScope({tenantId,userId,threadId}) and assertMemoryIdentity(ctx) utilities. Canonical keys must resolve to scope string tenantId:userId:threadId plus event key memory:{scope}:events and archive key memory:{scope}:archive. Create backend/src/agents/efficiency_plan/shared/memorySchema.js with Zod schemas for identity, write input, and persisted event record; enforce append-only revision semantics by requiring immutable revisionId on every write payload (D-10). Include deterministic error messages that can map to HTTP 400 via existing ApiError flow.</action>
  <verify>
    <automated>cd backend && node -e "const {assertMemoryIdentity,buildThreadScope}=require('./src/agents/efficiency_plan/shared/memoryKeys'); const ok=assertMemoryIdentity({tenantId:'t1',userId:'u1',threadId:'th1'}); if(buildThreadScope(ok)!=='t1:u1:th1') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Identity utilities expose canonical scope/key generation and throw deterministic validation failures for missing identity keys; provenance schemas compile and enforce D-09/D-10/D-11/D-12 contracts.</acceptance_criteria>
  <done>Memory key and schema contracts exist and are ready for service-layer consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sensitive-field redaction/tokenization utility and contract tests</name>
  <read_first>.planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md, backend/src/middleware/logging.middleware.js</read_first>
  <files>backend/src/agents/efficiency_plan/shared/redaction.js, backend/tests/helpers/memoryFixtures.js, backend/tests/memory.provenance.schema.test.js</files>
  <behavior>
    - Fields named token, apiKey, password, secret, authorization are never persisted raw (D-08).
    - Redaction utility is deterministic for equal input values to support testable provenance payloads.
    - Schema test validates both allowed and rejected provenance payload combinations.
  </behavior>
  <action>Create redactMemoryPayload(payload) in backend/src/agents/efficiency_plan/shared/redaction.js that deep-walks objects/arrays and replaces sensitive values with tokenized placeholders preserving field presence. Add backend/tests/helpers/memoryFixtures.js with valid/invalid identity and provenance fixtures. Add backend/tests/memory.provenance.schema.test.js covering: valid event write, missing identity rejection, missing required provenance fields rejection, and D-11 low-confidence/no-evidence allowed branch.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/memory.provenance.schema.test.js</automated>
  </verify>
  <acceptance_criteria>Redaction utility prevents raw sensitive fields from entering memory payloads, and automated tests prove provenance/identity validation behavior required for MEM-03 and OPS-02 trace readiness.</acceptance_criteria>
  <done>Contract-level memory foundation is guarded by deterministic tests.</done>
</task>

</tasks>

<verification>
- Run `cd backend && npm test -- --runInBand tests/memory.provenance.schema.test.js`.
- Confirm failing cases assert deterministic schema/identity errors suitable for centralized 400 handling.
- Confirm redaction test cases verify sensitive fields are transformed before persistence.
</verification>

<success_criteria>

- Canonical identity/key contract is implemented and test-covered.
- Provenance schema rules (including D-11 exception) are enforced by automation.
- Redaction contract exists and is reusable by persistence writes.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-memory-and-context-infrastructure/03-01-SUMMARY.md`
</output>
