---
phase: 03-shared-memory-and-context-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/package.json
  - backend/package-lock.json
  - backend/src/agents/efficiency_plan/shared/memoryStore.redis.js
  - backend/src/agents/efficiency_plan/shared/retrievalPlanner.js
  - backend/src/agents/efficiency_plan/shared/memoryService.js
  - backend/tests/memory.workspace.persistence.test.js
  - backend/tests/memory.retrieval.budget.test.js
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
must_haves:
  truths:
    - "Memory writes are append-only events scoped by tenant:user:thread and persisted in Redis."
    - "Agent context retrieval uses recent-window-first plus ranked historical recall under a hard token cap."
    - "If relevance retrieval returns no useful matches, system falls back to recent-window-only context."
  artifacts:
    - path: "backend/src/agents/efficiency_plan/shared/memoryStore.redis.js"
      provides: "Redis-backed append-only event persistence and retrieval primitives"
    - path: "backend/src/agents/efficiency_plan/shared/retrievalPlanner.js"
      provides: "D-13/D-14/D-15/D-16 retrieval strategy and token-budget enforcement"
    - path: "backend/src/agents/efficiency_plan/shared/memoryService.js"
      provides: "Single entrypoint for validated writes and bounded reads"
    - path: "backend/tests/memory.workspace.persistence.test.js"
      provides: "MEM-01 persistence contract coverage"
    - path: "backend/tests/memory.retrieval.budget.test.js"
      provides: "Token cap and fallback retrieval contract coverage"
  key_links:
    - from: "memoryService.writeEvent"
      to: "memoryStore.redis.appendEvent"
      via: "append-only persistence call"
      pattern: "appendEvent\("
    - from: "memoryService.buildAgentContext"
      to: "retrievalPlanner.selectWithinTokenBudget"
      via: "bounded retrieval assembly"
      pattern: "selectWithinTokenBudget\("
    - from: "retrievalPlanner"
      to: "js-tiktoken"
      via: "hard token counting"
      pattern: "encoding_for_model|Tiktoken"
---

<objective>
Implement the Redis-backed shared-memory write/read engine and bounded retrieval planner that satisfy Phase 3 persistence and continuity behaviors.

Purpose: Deliver MEM-01/MEM-02 core runtime mechanics with deterministic contracts from Plan 03-01.
Output: Working memory store/service/retrieval modules plus requirement-mapped automated tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-RESEARCH.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-VALIDATION.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-01-PLAN.md
@backend/src/services/CacheService.js
@backend/package.json

<interfaces>
From backend/src/services/CacheService.js:
```javascript
const cacheService = new CacheService();
module.exports = cacheService;
```

Required retrieval policy (D-13..D-16):

```text
recent-window-first (default 12) + relevance-ranked historical items,
hard token cap enforced before prompt assembly,
fallback to recent-only when relevance yields no useful matches.
```

Required retention policy (D-05, D-07):

```text
active thread history retained for 30 days,
archive written as cold storage,
archive not queried at runtime in Phase 3.
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Redis append-only memory store and validated memory service</name>
  <read_first>backend/src/services/CacheService.js, .planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md, backend/src/middleware/errorHandler.js</read_first>
  <files>backend/src/agents/efficiency_plan/shared/memoryStore.redis.js, backend/src/agents/efficiency_plan/shared/memoryService.js</files>
  <behavior>
    - writeEvent validates identity + provenance and appends immutable event records with revisionId (D-09, D-10).
    - Active thread stream uses 30-day retention and archive sink writes occur for rollover events (D-05, D-07).
    - Cross-thread reads require same tenantId/userId guard and explicit query path, never unrestricted merge (D-03).
  </behavior>
  <action>Create backend/src/agents/efficiency_plan/shared/memoryStore.redis.js with appendEvent, listRecentEvents, listHistoricalEvents, and archiveEvent operations using Redis primitives through CacheService.client. Use key contract from Plan 03-01 and store each event as immutable record including requestId/runId when present (D-12). Create backend/src/agents/efficiency_plan/shared/memoryService.js exposing writeEvent(input), getRecent(scope,{limit}), and getHistorical(scope,query,options). Enforce D-04 missing-key failures as ApiError(400) and enforce append-only write semantics by forbidding in-place mutation APIs.</action>
  <verify>
    <automated>cd backend && node -e "const svc=require('./src/agents/efficiency_plan/shared/memoryService'); if(typeof svc.writeEvent!=='function'||typeof svc.getRecent!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Memory service exposes write/read APIs with enforced identity/provenance contracts, append-only revisions, retention configuration, and no mutable overwrite path.</acceptance_criteria>
  <done>Shared-memory persistence core is implemented with deterministic API and invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded retrieval planner with hard token budgeting</name>
  <read_first>.planning/phases/03-shared-memory-and-context-infrastructure/03-RESEARCH.md, backend/package.json</read_first>
  <files>backend/package.json, backend/package-lock.json, backend/src/agents/efficiency_plan/shared/retrievalPlanner.js</files>
  <behavior>
    - Default recent window is exactly 12 events (D-14).
    - Planner applies ranking + truncation and never exceeds configured token budget (D-15).
    - Planner returns recent-only when historical relevance returns empty/irrelevant set (D-16).
  </behavior>
  <action>Add js-tiktoken dependency and implement backend/src/agents/efficiency_plan/shared/retrievalPlanner.js with composeAgentContext({recentEvents,historicalEvents,query,tokenBudget,model}). Implement deterministic ranking (recency-weighted lexical relevance), token counting via js-tiktoken, and truncation loop that stops before exceeding budget. Set defaults: recentLimit=12, tokenBudget=6000 unless caller override. Explicitly avoid adaptive learned ranking (deferred idea) and archive readback query paths.</action>
  <verify>
    <automated>cd backend && npm ls js-tiktoken && node -e "const p=require('./src/agents/efficiency_plan/shared/retrievalPlanner'); if(typeof p.composeAgentContext!=='function') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Retrieval planner enforces hard token limits with deterministic selection order and provides guaranteed recent-only fallback behavior.</acceptance_criteria>
  <done>Bounded retrieval engine is implemented and ready for orchestration wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add persistence and token-budget tests for MEM-01 and MEM-02 contracts</name>
  <read_first>backend/tests/helpers/memoryFixtures.js, backend/src/agents/efficiency_plan/shared/memoryService.js, backend/src/agents/efficiency_plan/shared/retrievalPlanner.js</read_first>
  <files>backend/tests/memory.workspace.persistence.test.js, backend/tests/memory.retrieval.budget.test.js</files>
  <behavior>
    - Persistence test verifies append-only revisions and thread-scoped reads.
    - Retrieval test verifies hard budget truncation and recent-only fallback on no-match historical recall.
    - Test fixtures remain deterministic with fixed timestamps/revisionIds.
  </behavior>
  <action>Create backend/tests/memory.workspace.persistence.test.js to assert writeEvent persists immutable events with required provenance and that recent/historical reads remain thread scoped. Create backend/tests/memory.retrieval.budget.test.js to assert composeAgentContext never exceeds the configured token budget and returns recent window when historical candidates are non-useful. Use deterministic fixtures from tests/helpers/memoryFixtures.js and no live network calls.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/memory.workspace.persistence.test.js tests/memory.retrieval.budget.test.js</automated>
  </verify>
  <acceptance_criteria>Automated tests prove persistence correctness, append-only behavior, token-budget enforcement, and fallback retrieval strategy expected by MEM-01/MEM-02.</acceptance_criteria>
  <done>Core memory runtime behaviors are guarded by repeatable automated tests.</done>
</task>

</tasks>

<verification>
- Run `cd backend && npm test -- --runInBand tests/memory.workspace.persistence.test.js tests/memory.retrieval.budget.test.js`.
- Confirm retrieval tests enforce token cap with deterministic truncation.
- Confirm persistence tests verify immutable revision events and scoped thread reads.
</verification>

<success_criteria>

- Redis-backed append-only memory service is implemented and contract-validating.
- Retrieval planner provides bounded context assembly with required defaults and fallback.
- MEM-01 and MEM-02 baseline behavior is automatically verified.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-memory-and-context-infrastructure/03-02-SUMMARY.md`
</output>
