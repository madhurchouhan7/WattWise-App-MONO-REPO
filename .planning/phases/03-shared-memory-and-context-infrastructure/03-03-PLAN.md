---
phase: 03-shared-memory-and-context-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - backend/src/agents/efficiency_plan/state.js
  - backend/src/agents/efficiency_plan/collaborative.index.js
  - backend/src/controllers/ai.controller.js
  - backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js
  - backend/src/middleware/logging.middleware.js
  - backend/tests/memory.context.continuity.test.js
  - backend/tests/memory.trace.correlation.test.js
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - OPS-02
must_haves:
  truths:
    - "Each collaborative agent turn receives bounded memory context hydrated from shared workspace."
    - "Memory events are trace-correlated with requestId and runId through controller/orchestrator flow."
    - "Structured logs and response metadata expose execution trace linkage for audit and debugging."
  artifacts:
    - path: "backend/src/controllers/ai.controller.js"
      provides: "Identity/run context injection into memory service"
    - path: "backend/src/agents/efficiency_plan/collaborative.index.js"
      provides: "Per-turn memory hydration and write-back integration"
    - path: "backend/src/agents/efficiency_plan/state.js"
      provides: "State channels for memory context and memory event refs"
    - path: "backend/tests/memory.context.continuity.test.js"
      provides: "MEM-02 continuity assertions across turns"
    - path: "backend/tests/memory.trace.correlation.test.js"
      provides: "OPS-02 trace/log correlation assertions"
  key_links:
    - from: "ai.controller.getEfficiencyPlan"
      to: "memoryService"
      via: "identity + run metadata handoff"
      pattern: "tenantId|userId|threadId|runId"
    - from: "collaborativePlanApp.invoke"
      to: "retrievalPlanner.composeAgentContext"
      via: "pre-node memory hydration"
      pattern: "composeAgentContext\("
    - from: "logging.middleware"
      to: "memory events"
      via: "structured log fields requestId/runId/threadId"
      pattern: "requestId|runId|threadId"
---

<objective>
Wire shared memory and trace correlation into collaborative orchestration and lock continuity/observability behavior with integration tests.

Purpose: Complete runtime integration so Phase 3 memory infrastructure is active in real request flow, not only in isolated modules.
Output: Controller/orchestrator/state/logging integration and final MEM/OPS test coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-VALIDATION.md
@.planning/phases/03-shared-memory-and-context-infrastructure/03-02-PLAN.md
@backend/src/controllers/ai.controller.js
@backend/src/agents/efficiency_plan/collaborative.index.js
@backend/src/agents/efficiency_plan/state.js
@backend/src/middleware/logging.middleware.js

<interfaces>
From backend/src/controllers/ai.controller.js:
```javascript
const { requestedMode, executionPath } = resolveOrchestrationMode(req);
const resultState = await selectedApp.invoke(initialState);
const responseEnvelope = buildPlanResponseEnvelope({ finalPlan: plan, requestedMode, executionPath, requestId: req.id });
```

From backend/src/agents/efficiency_plan/state.js:

```javascript
const StateAnnotation = Annotation.Root({
  userData: Annotation(...),
  weatherContext: Annotation(...),
  finalPlan: Annotation(...),
});
```

From backend/src/middleware/logging.middleware.js:

```javascript
const logData = { requestId, method: req.method, url: req.originalUrl };
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate shared memory context into controller, collaborative orchestration, and state channels</name>
  <read_first>backend/src/controllers/ai.controller.js, backend/src/agents/efficiency_plan/collaborative.index.js, backend/src/agents/efficiency_plan/state.js, .planning/phases/03-shared-memory-and-context-infrastructure/03-CONTEXT.md</read_first>
  <files>backend/src/controllers/ai.controller.js, backend/src/agents/efficiency_plan/collaborative.index.js, backend/src/agents/efficiency_plan/state.js</files>
  <behavior>
    - Controller supplies tenantId, userId, threadId, runId, requestId to collaborative path memory APIs (D-02, D-12).
    - Collaborative invoke hydrates context before agent execution and appends post-turn memory events after execution (MEM-01, MEM-02).
    - State annotation includes memoryContext and memoryEventRefs channels for inter-node continuity.
  </behavior>
  <action>Update ai.controller to extract identity fields from authenticated request/user payload and reject missing tenant/user/thread with ApiError(400) in collaborative mode (D-04). Update collaborative.index.js to call memoryService.getRecent/getHistorical + retrievalPlanner.composeAgentContext before node logic, then memoryService.writeEvent after node output with provenance fields from D-09. Extend state.js annotation with memoryContext (array) and memoryEventRefs (array of event IDs/revision IDs) reducers. Keep legacy path behavior unchanged per compatibility constraints.</action>
  <verify>
    <automated>cd backend && node -e "const {StateAnnotation}=require('./src/agents/efficiency_plan/state'); if(!StateAnnotation) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Collaborative execution path is memory-aware end-to-end, missing required identity fails deterministically, and state schema carries memory context references across transitions.</acceptance_criteria>
  <done>Runtime memory continuity is wired into collaborative execution lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add trace-correlated envelope and structured memory logging</name>
  <read_first>backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js, backend/src/middleware/logging.middleware.js, .planning/phases/03-shared-memory-and-context-infrastructure/03-RESEARCH.md</read_first>
  <files>backend/src/agents/efficiency_plan/orchestrators/responseEnvelope.js, backend/src/middleware/logging.middleware.js</files>
  <behavior>
    - Response metadata includes memoryTrace object with requestId, runId, threadId for collaborative runs.
    - Memory write/read logs emit structured records including eventType, scope, revisionId, requestId, runId.
    - OPS-02 trace fields are present in logs even on memory retrieval fallback decisions.
  </behavior>
  <action>Enhance buildPlanResponseEnvelope to include metadata.memoryTrace containing requestId/runId/threadId when available while preserving existing Phase 2 metadata keys. Update logging middleware to emit additional structured entries for memory operations (memory_write, memory_read, memory_fallback) with requestId/runId/threadId/revisionId and tokenBudgetUsed fields. Do not change legacy non-memory route logging behavior outside additive fields.</action>
  <verify>
    <automated>cd backend && node -e "const {buildPlanResponseEnvelope}=require('./src/agents/efficiency_plan/orchestrators/responseEnvelope'); const out=buildPlanResponseEnvelope({finalPlan:{},requestedMode:'collaborative',executionPath:'collaborative',requestId:'req1',runId:'run1',threadId:'th1'}); if(!out.metadata.memoryTrace||out.metadata.memoryTrace.runId!=='run1') process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>Collaborative responses and logs expose correlated trace metadata for memory operations without breaking existing metadata contract keys.</acceptance_criteria>
  <done>Traceability surface for OPS-02 is wired across response and logs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add continuity and trace-correlation integration tests and run Phase 3 memory suite</name>
  <read_first>backend/tests/helpers/memoryFixtures.js, backend/src/controllers/ai.controller.js, backend/src/agents/efficiency_plan/collaborative.index.js</read_first>
  <files>backend/tests/memory.context.continuity.test.js, backend/tests/memory.trace.correlation.test.js</files>
  <behavior>
    - Continuity test proves prior turn artifacts are available in subsequent collaborative turn context.
    - Trace test proves requestId/runId/threadId are present in memory event records/log metadata.
    - Missing identity test path returns centralized 400 payload with requestId.
  </behavior>
  <action>Create backend/tests/memory.context.continuity.test.js to run two-turn collaborative invocation and assert turn-2 context contains selected turn-1 artifacts under token bounds. Create backend/tests/memory.trace.correlation.test.js to assert memory writes and response envelope metadata include requestId/runId/threadId and revisionId, plus structured fallback log entry when historical recall yields no matches. Execute full memory test set after adding tests.</action>
  <verify>
    <automated>cd backend && npm test -- --runInBand tests/memory.context.continuity.test.js tests/memory.trace.correlation.test.js tests/memory.provenance.schema.test.js tests/memory.workspace.persistence.test.js tests/memory.retrieval.budget.test.js</automated>
  </verify>
  <acceptance_criteria>End-to-end tests validate cross-turn memory continuity and trace/log correlation guarantees required by MEM-02 and OPS-02.</acceptance_criteria>
  <done>Phase 3 memory and observability contracts are enforced by automated integration coverage.</done>
</task>

</tasks>

<verification>
- Run `cd backend && npm test -- --runInBand tests/memory.context.continuity.test.js tests/memory.trace.correlation.test.js tests/memory.provenance.schema.test.js tests/memory.workspace.persistence.test.js tests/memory.retrieval.budget.test.js`.
- Run `cd backend && npm test -- --runInBand tests/ai.compat.legacy.test.js tests/ai.compat.routing.test.js tests/ai.compat.errors.test.js` to confirm Phase 2 compatibility remains intact.
- Confirm collaborative responses include metadata.memoryTrace for correlated runs.
</verification>

<success_criteria>

- Memory continuity is active across collaborative turns.
- Trace IDs correlate controller responses, memory events, and structured logs.
- Full Phase 3 requirement-mapped memory suite passes.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-shared-memory-and-context-infrastructure/03-03-SUMMARY.md`
</output>
