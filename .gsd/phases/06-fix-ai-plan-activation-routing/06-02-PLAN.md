---
phase: 06-fix-ai-plan-activation-routing
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart
  - wattwise_app/lib/feature/plans/screens/plans_screen.dart
autonomous: true
requirements: [REQ-06]
must_haves:
  truths:
    - "User transitions directly from PlanReadyScreen to ActivePlanScreen upon activation."
    - "No visual fallback to DesignPlanScreen occurs during the auth state refresh."
  artifacts:
    - path: "wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart"
      provides: "Direct navigation to ActivePlanScreen with local data"
    - path: "wattwise_app/lib/feature/plans/screens/plans_screen.dart"
      provides: "Loading guard for auth state transitions"
  key_links:
    - from: "wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart"
      to: "wattwise_app/lib/feature/plans/screens/active_plan_screen.dart"
      via: "Navigator.pushAndRemoveUntil"
---

# PLAN.md - Phase 6: Navigation UI Refinement

<objective>
Fix the AI Plan Activation routing issue by ensuring PlanReadyScreen pushes directly to ActivePlanScreen and PlansScreen handles the loading state gracefully.

Purpose: Prevent the 'Design Plan' flash and ensure immediate UI feedback.
Output: Smooth navigation transition to the active plan dashboard.
</objective>

<execution_context>
@F:/Flutter Projects/WattWise Mono Repo/.gemini/get-shit-done/workflows/execute-plan.md
@F:/Flutter Projects/WattWise Mono Repo/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.gsd/STATE.md
@.gsd/ROADMAP.md
@.gsd/phases/06-fix-ai-plan-activation-routing/06-01-SUMMARY.md
@wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart
@wattwise_app/lib/feature/plans/screens/plans_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Direct Navigation in PlanReadyScreen</name>
  <files>wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart</files>
  <action>
    Update the 'Activate Plan' button logic:
    1. Capture the result from `saveActivePlan` (which now returns the updated data per Plan 01).
    2. Instead of `popUntil`, use `Navigator.of(context).pushAndRemoveUntil` to navigate to `ActivePlanScreen`.
    3. Pass the newly activated plan data directly to `ActivePlanScreen`.
    4. Ensure `clearPlan()` and `ref.invalidate(authStateProvider)` are still called to clean up the staging area and trigger a background refresh.
    
    Ref: Per research, pushAndRemoveUntil((route) => route.isFirst) ensures we keep the bottom nav but replace the stack above it.
  </action>
  <verify>
    <automated>flutter analyze wattwise_app/lib/feature/plans/screens/plan_ready_screen.dart</automated>
  </verify>
  <done>PlanReadyScreen navigates directly to ActivePlanScreen without waiting for auth refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen Loading Guards in PlansScreen</name>
  <files>wattwise_app/lib/feature/plans/screens/plans_screen.dart</files>
  <action>
    Modify `PlansScreen` to handle the transition gap:
    1. In the `data` handler for `aiPlanProvider`, enhance the `isLoading` check. 
    2. If `authUser` is null AND `authState.isLoading` is true, return the loading shimmer.
    3. Crucially, if `plan` is null but `authState` is still loading/refreshing, do NOT immediately return `DesignPlanScreen`. Instead, return the shimmer or a placeholder to wait for the `authUser` to resolve.
    
    This prevents the race where `plan` clears before `authUser.activePlan` is populated.
  </action>
  <verify>
    <automated>flutter analyze wattwise_app/lib/feature/plans/screens/plans_screen.dart</automated>
  </verify>
  <done>PlansScreen waits for authState to finish loading before falling back to DesignPlanScreen.</done>
</task>

</tasks>

<verification>
1. Generate an AI Plan.
2. Click 'Activate Plan'.
3. Observe the transition: It should be immediate and direct.
4. Verify that no 'Design Plan' (Step 1) screen is visible at any point.
</verification>

<success_criteria>
- Navigation to ActivePlanScreen is instantaneous.
- No flickering or flash of incorrect screens.
- Auth state synchronization happens in the background without affecting the user's active view.
</success_criteria>
